language: node_js
os:
  - windows
node_js:
  - "11"
  - "10"
before_install:
  - if [[ "$TRAVIS_OS_NAME" == "windows" ]]; then powershell Get-ExecutionPolicy -List; powershell Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope LocalMachine; fi
script:
  - npm run test-chrome
